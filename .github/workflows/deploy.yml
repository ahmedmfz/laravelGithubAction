name: Laravel Deploy Productions

on:
  push:
    branches:
      - main

jobs:
  deploy-laravel:
    name: Laravel Deploy Action
    runs-on: ubuntu-latest

    steps:
      - name: set up Ec2 Ssh Access
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.LARAVEL_ACTION_PRIVATE }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.LARAVEL_ACTION_IP }}  >> ~/.ssh/known_hosts

      - name: Deploy Laravel on Ec2
        run: |
         ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@${{ secrets.LARAVEL_ACTION_IP }} << 'EOF'
         set -e

         echo 'Navigate to laravelfolder'
#         cd /var/www/laravel

#         echo 'Prepare Writeable Directory'
#         sudo chown -R ubuntu:ubuntu storage bootstrap/cache
#         sudo chmod -R 755 storage bootstrap/cache


